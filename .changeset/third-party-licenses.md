---
"@ibitsuki0296/md-book": patch
---

Ship `THIRD_PARTY_LICENSES.txt` (licences of the packages bundled into the CDN/CJS builds), reference it from a banner in `md-book.global.js`, and copy it next to the runtime in `md-book build` output.
