# Changesets

This folder holds [changesets](https://github.com/changesets/changesets). Run
`npx changeset` to describe a change; `npx changeset version` applies pending
changesets to the version and CHANGELOG; `npm run release` builds and publishes.
