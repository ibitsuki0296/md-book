pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,js,mjs,json,css}"
      run: npx biome check --no-errors-on-unmatched {staged_files}
    typecheck:
      glob: "*.ts"
      run: npm run typecheck

pre-push:
  commands:
    test:
      run: npm test
